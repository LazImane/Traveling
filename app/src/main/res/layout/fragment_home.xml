<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- SEARCH BAR -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:background="@drawable/search_btn"
        android:gravity="center_vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="10dp">

        <ImageView
            android:layout_width="48dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_search"
            android:layout_marginEnd="8dp" />

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="@string/search_hint"
            android:textColorHint="@color/grey"
            android:textSize="15sp"
            android:background="@null"
            android:inputType="text" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/btnVoiceSearch"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_mic"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Voice search" />

    </LinearLayout>

    <!-- FILTER CHIPS -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:layout_marginBottom="12dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/filterNature"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/nature"
                android:textSize="14sp"
                android:background="@drawable/selectable_filter_bt"
                android:textColor="@color/black"
                android:layout_marginEnd="8dp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/filterCity"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/city"
                android:textSize="14sp"
                android:background="@drawable/selectable_filter_bt"
                android:textColor="@color/black"
                android:layout_marginEnd="8dp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/filterMuseums"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/museum"
                android:textSize="14sp"
                android:background="@drawable/selectable_filter_bt"
                android:textColor="@color/black"
                android:layout_marginEnd="8dp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/filterShops"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/shop"
                android:textSize="14sp"
                android:background="@drawable/selectable_filter_bt"
                android:textColor="@color/black"
                android:layout_marginEnd="8dp" />

            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/filterAround"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/around_me"
                android:textSize="14sp"
                android:background="@drawable/selectable_filter_bt"
                android:textColor="@color/black" />

        </LinearLayout>
    </HorizontalScrollView>

    <!-- Progress bar -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:visibility="gone"
        android:indeterminateTint="@color/chip_green" />


    <!-- SCROLLABLE CONTENT (posts go here later) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPosts"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="4dp"
        android:clipToPadding="false" />

</LinearLayout>